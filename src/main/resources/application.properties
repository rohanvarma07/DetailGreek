spring.application.name=Backend

# MySQL Database Connection for Ceramic Products
# MySQL Database Connection for Ceramic Products
# Database Connection
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/ceramic_product
spring.datasource.username=root
spring.datasource.password=rohan4444
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA and Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Optional if using a different schema like "cermaic_product"
 spring.jpa.properties.hibernate.default_schema=cermaic_product

# File upload settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Image storage path
app.upload.dir=uploads/images/

